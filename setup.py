"""
Setup script for OpticalCT.

Run with pip install -e . to setup the project directory as a package in
your environment.
"""

from distutils.core import setup
# from setuptools import find_packages

setup(
    name="opticalct",
    version="0.0.1",
    description="CT reconstruction for Jon Bumstead's Desktop CT scanner"
)
